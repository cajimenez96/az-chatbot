{
  "skills": [],
  "conventions": [
    {
      "trigger": "bot",
      "rules": "The bot uses WPPConnect. On Windows, ensure headless: false is used if debugging connection. The bot service in API and the bot process must sync via internal API calls."
    }
  ]
}
